#!/bin/bash
# GCP Credentials Template
# Copy this file to gcp-credentials.sh and fill in your values.
# The actual gcp-credentials.sh file is git-excluded.

# Path to your GCP service account JSON key file
export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.gcp/service-account-key.json"
export GCP_PROJECT_ID="your-project-id"
